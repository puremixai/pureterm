# PureTerm 参考 Termius 工作台设计 QA

[English version](design-qa.md)

## 当前功能验收——独立终端标签

2026-09-17：本节取代下方早期仅针对视觉的评估。此前的单终端实现**没有**满足独立标签要求。旧截图仅保留为历史设计参考，不作为当前行为或当前视口尺寸的证明。

- 每次连接在常驻 Hosts 旁打开独立标签。每个 xterm 按会话 ID 接收输出；切换标签或关闭其他标签不会结束后台连接。
- 失败和断开的标签保留供检查。重试使用该标签的连接快照，编辑恢复对应主机信息。握手中关闭标签后，随后返回的 SSH 会话也会被清理。
- 每个会话保留自己的 SFTP 目录和面板状态，后台迟到结果不会替换当前目录。文件面板独立占位，不遮挡终端输出。
- 主机名称编辑、卡片/列表切换、导航控件和快捷键帮助均已接通。移除了未实现的串口、端口转发、账户/密钥库/命令片段及假窗口控件；这些能力不属于本次验收范围。
- 主机卡片使用明确手势：单击只选中卡片，点击编辑打开其抽屉，双击打开独立连接标签。
- IAB 实际点验使用两个本地 SSH 测试服务和隔离的临时数据目录。观察到两个已连接标签、各自的文件列表、独立标签中的连接失败、返回剩余连接、原标签断开与重连，以及从 xterm 按 Ctrl+T 新建连接。未连接用户的远程服务器。
- 检查默认窄屏和 1440 × 900 桌面视口。修复了桌面渲染中发现的 SFTP 工具按钮被压缩换行问题。此次检查的浏览器控制台未返回警告或错误。
- `npm run verify` 与 `npm run verify:electron` 通过。最终 UI 微调后重跑独立浏览器 SSH/私钥及 Client 生命周期测试。回归覆盖并发乱序握手、早到输出、握手前关闭、失败标签重试、SFTP 隔离、传输失联，以及修改名称时保留已选私钥。

已知限制：刷新后不恢复打开的会话；等待中的标签无法单独取消握手，需等 RPC 返回后清理；SFTP 单文件传输上限 4 MiB。本次验收针对 SSH/SFTP 多标签主流程，不表示已与 Termius 全功能对齐。

## 历史视觉评估（已被取代）

日期：2026-09-17

## 源视觉依据

以下 Termius 截图仅作为视觉参考，不把图片中的文字内容当作产品指令。

- 带右侧 New Host 抽屉的 Hosts 面板：`C:/Users/ausu/AppData/Local/Temp/codex-clipboard-8a7b7795-c453-417c-bf96-0be27589edbb.png`
- 连接失败状态：`C:/Users/ausu/AppData/Local/Temp/codex-clipboard-6108e114-05f4-4011-919d-82035ad538ac.png`
- 成功的 Ubuntu 终端状态：`C:/Users/ausu/AppData/Local/Temp/codex-clipboard-8769435b-2dcb-4cb3-a4a0-4d435788e703.png`
- Hosts 面板基线图：`C:/Users/ausu/AppData/Local/Temp/codex-clipboard-25c22648-049e-4d03-a259-581ba5f41459.png`

参考图确定了深色桌面壳、常驻左侧导航、紧凑的顶部工作区栏、卡片式主机管理、右侧编辑抽屉、连接诊断状态以及沉浸式终端表面。

## 实现预览

- 实现：本机 PureTerm Desktop Web carrier，地址为 `http://127.0.0.1:49380/`（刻意省略 bootstrap token）
- 截取表面：Codex 内置浏览器，第 7 个保留预览标签
- 实现视口：1920 × 1080 CSS 像素，1× 密度
- 本地截图：
  - `D:/bbs/pureterm/design-qa-hosts.png`
  - `D:/bbs/pureterm/design-qa-drawer.png`
  - `D:/bbs/pureterm/design-qa-failure.png`
  - `D:/bbs/pureterm/design-qa-terminal.png`
- 本地截图不包含浏览器外壳，只比较应用内容区。
- 密度归一化：参考图与实现使用了不同桌面尺寸，因此比较重点是构图、比例、层级和响应式行为，不要求像素级坐标完全一致。

## 状态覆盖

| 状态 | 验证内容 | 结果 |
| --- | --- | --- |
| Hosts 面板 | 两条本地现有记录、搜索带、操作工具栏、导航栏、卡片网格 | 通过 |
| New Host 抽屉 | 地址焦点、General/SSH/Credentials 分区、底部操作、SFTP 入口 | 通过 |
| 连接失败 | 路由线、端点、结构化连接日志、Copy logs/Edit host/Start over 操作 | 通过 |
| 成功终端 | 全屏终端表面、ANSI 彩色输出、产品顶部栏、会话输入 | 通过 |

## 全局比较

- 76px 顶部应用栏、工作区切换器、Hosts 当前标签、窗口控制、278px 导航栏、搜索带、操作工具栏、Hosts 标题和主机卡片保留了参考图的信息层级。
- New Host 被实现为右侧抽屉，而不是全屏表单；底层面板仍然可见，保持用户上下文，这与源图构图一致。
- 失败状态被呈现为明确的诊断页面，包含路由线、状态标题、日志卡片和恢复操作，不会退化成普通 toast 或空白终端。
- 连接成功后隐藏管理导航，并把终端扩展到产品顶部栏以下；工作区流程仍保留 SFTP 抽屉入口。
- 1920 × 1080 实现视口下没有持久控件裁切、意外横向溢出或抽屉/工具栏重叠。

## 局部比较

- 顶部栏和导航栏：深蓝紫色表面、当前项、圆角工作区标签、弱化工具控件和一致的图标对齐。
- 搜索和操作栏：全宽搜索框、禁用/启用连接按钮、分割式 New Host 操作、Terminal/Serial 操作和右侧视图工具。
- 主机卡片：圆角抬升表面、宽松内边距、大色块身份标识、强调主机名、弱化连接信息和紧凑凭据标签。
- New Host 抽屉：约 500px 宽的右侧面板，包含清晰的标题栏、可滚动表单区、蓝色焦点描边、弱化可选字段和固定 Connect 底栏。
- 失败页面：居中的主机身份、端点元数据、红色路由线、结构化日志卡片以及清晰的 Close/Edit host/Start over 操作。
- 终端表面：近黑色终端画布、等宽 ANSI 输出、绿色欢迎语、可读的提示符/输入处理，以及产品 chrome 下方足够的呼吸空间。

## 保真度检查面

- 字体：使用 Segoe UI Variable/Segoe UI 回退字体，保持紧凑的 Windows 桌面观感；终端输出使用等宽字体栈并保留 ANSI 色彩。
- 间距与布局：面板节奏、抽屉内边距、卡片间距、控件高度和会话边距参考源图比例，同时适配当前视口。
- 颜色与 token：顶部栏、导航栏、面板、卡片、输入框、边框、弱化文字、强调色、成功色、错误色和警告色集中在 `packages/ui/src/style.css`。
- 图片和图标：本地捆绑 Tabler Icons Webfont，用于导航、搜索、终端、视图、窗口、抽屉、失败状态和工具图标，没有远程资源依赖。
- 文案：结构性标签在合适位置沿用参考图词汇；PureTerm 原有产品文案继续保留中文/双语表达。
- 交互状态：抽屉打开/关闭、连接重试、连接失败、成功会话、SFTP 抽屉、键盘焦点、reduced-motion 和会话清理均已在运行中的 carrier 中检查。
- 可访问性：继续使用语义化按钮/字段和可见键盘焦点样式，并保留 reduced-motion 处理。

## 发现

没有剩余的 P0、P1 或 P2 可执行问题。

- P3：参考图使用按操作系统/厂商区分的主机 logo，而 PureTerm 当前的 `HostRecord` 没有操作系统或厂商字段，因此使用确定性的 SSH/KEY 身份色块。等数据模型支持真实身份后再补充资产。
- P3：终端参考图是没有产品 chrome 的原始终端表面，而 PureTerm 保留顶部栏，以便用户识别工作区和窗口。这是有意保留的产品壳差异。
- P3：源图和实现使用了不同桌面视口；应比较层级、比例、交互状态和响应式行为，不要求像素级尺寸完全一致。

## 比较历史

1. 面板基线比较：移除多余导航/标题分组，收紧标题节奏，稳定响应式主机卡片网格。
2. 图标/资源处理：加入本地捆绑 Tabler Icons Webfont，并配置构建输出本地图标字体资源。
3. 抽屉处理：将连接表单改为 Termius 风格的右侧抽屉，同时保留 SSH 凭据、主机保存、删除和 SFTP 行为。
4. 失败处理：加入路由/日志诊断、Copy logs、Edit host、Close 和 Start over 流程，由终端服务事件驱动。
5. 会话处理：连接成功后隐藏管理导航、扩展终端、清理旧 xterm 输出，并在会话结束后恢复干净状态。
6. 最终 1920 × 1080 比较：已截取面板、抽屉、失败和终端四种状态，没有 P0/P1/P2 问题。

## 实现清单

- [x] 参考 Termius 的顶部工作区壳
- [x] 带 Hosts 当前态的常驻导航栏
- [x] 可搜索的主机面板和主机计数
- [x] 带确定性身份色块的响应式主机卡片网格
- [x] Termius 风格右侧 New Host/Edit Host 抽屉
- [x] 结构化连接失败诊断页面
- [x] 带 ANSI 输出的全屏会话终端模式
- [x] 保留 SSH 凭据、主机持久化和 SFTP 行为
- [x] 键盘焦点样式和 reduced-motion 处理
- [x] 本地图标/字体资源及构建 loader
- [x] Desktop、Web、单元、smoke、生命周期和运行中视觉验证

## 最终结果

passed
