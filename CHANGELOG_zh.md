# PureTerm

[English version](CHANGELOG.md)

PureTerm 的所有重要变更都记录在这里。本文件遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) 并使用 [语义化版本](https://semver.org/)。

`[Unreleased]` 区段用于记录已经合并但尚未发布的变更。创建版本 tag 前，将这些条目移到带日期的版本区段。发布工作流要求对应版本区段存在，否则拒绝该 tag。

## [Unreleased]

### Added

- 增加完整 Keychain 密钥库：支持导入、粘贴、拖放、私钥及口令校验、生成公钥与指纹、搜索、卡片/列表切换、安全编辑和删除保护。主机可选择已保存密钥并在独立终端标签中认证连接；Desktop 使用原子写入的系统加密密钥库，Web 密钥及主机关联按客户端隔离且不落盘。
- 增加会话资源监控。每个会话在工具栏下面多出一行，默认折叠，每 5 秒通过一次 Linux 探测报告远端主机的 CPU、内存、负载、根文件系统占用、网络吞吐和运行时间。只有在标签「被选中、已连接、页面可见、已展开、未暂停」时才采集，折叠、暂停、页面隐藏、切换标签、断开或卸载都会停止，所以折叠的会话对远端零开销。远端没有提供的指标留空并保留原因，而不是读成 0；远端不是 Linux 时报「不支持监控」；CPU 与网络在第二个样本到达前显示「预热中」。状态栏的 cipher 与 host key 两格也填上了，来源是 SSH 握手而不是这一行。

### Changed

- **0.x 内部集成破坏性变更：**移除 Desktop 的 `window.sshAPI` 和 SSH 业务 IPC，以最小的 `window.puretermDesktop` 负责启动信息/就绪，并通过子进程拥有的共享回环 Web Host 处理 SSH/SFTP、主机和 Keychain。Electron 窗口从 `pureterm-app://app/` 加载；既有 SSH 数据和面向用户的操作仍可使用。独立 Web 保持独立，`SSH_CORDIS_NO_WEB_CARRIER=1` 仅关闭附带浏览器访问。
- 以紧凑的 PureTerm 顶栏替代原生标题栏；Windows/Linux 菜单栏自动隐藏，同时保留其快捷键能力。
- 将创建主机收敛为唯一的「新建主机」操作，移除顶部栏、导航、搜索、Terminal 与 Ctrl/Cmd+T 的重复入口。
- 明确主机卡片操作：单击只选中，点击「编辑」打开主机抽屉，双击打开新的终端标签。
- 每次 SSH 连接在 Hosts 旁打开独立终端标签，隔离输出、断开/关闭/重试及 SFTP 状态；正确处理并发握手、已关闭标签的迟到结果，以及 WebSocket 失联时全部会话的收尾。
- 支持编辑主机名称、卡片/列表切换和快捷键帮助，移除未实现的占位操作；文件面板在终端下方独立占位，不遮挡提示符。
- 将共享 PureTerm 工作台重构为成熟的深色 Hosts 面板：加入参考 Termius 的导航栏与工具栏、可搜索的主机卡片和聚焦式连接工作区，同时保留主机列表、SFTP 面板的响应式表现与键盘焦点状态。
- 将维护中的 Markdown 文档统一为英文优先，同时保留完整中文翻译。
- 增加英文优先的开发说明及对应中文翻译。
- 增加 `VERSION.txt` 作为源码基准，并生成界面版本和变更日志元数据。
- 界面改用 Inter 渲染，字体随包本地分发，页面不再发起任何远程字体请求；等宽文字统一由一套字体栈解析，取代此前多处手工拷贝的副本。文字字重限制在 400/500/600/700：越界的 650/750/800 无法由中文与系统回退字面渲染，它们只会以合成粗体回应 13px 文字。
- 终端改用中性 ANSI 色板，画布比此前更暗。终端里的黑字不再与背景色完全相同，弱化提示符颜色现在相对画布达到 3:1。
- 界面全部颜色改由 token 解析。体系位于 `packages/ui/src/styles/tokens.css`，其中同时持有深色与浅色两套分组，样式表里再没有别的文件可以书写颜色。文字、强调色与语义色都按它们实际绘制的底面计量并受 WCAG AA 约束，语义色标签包括其自身着色底在内；半透明的填充、边框与遮罩档必须携带被着色那个颜色的三分量，并逐主题断言。
- 界面改用中性石墨色板渲染。改版前的蓝紫表面、`#a7c4ff` 强调色与半透明细线全部退场，保存它们的受守卫登记簿也一并删除：六级石墨阶梯承担层级，唯一强调色承担状态，分隔依靠细线而不是阴影。浅色分组已经完整并处于测试之下，但仍不可达 —— 写入它的控件随外壳重建一起发布。
- 住在样式表之外的颜色与它所匹配的阶梯同步。Electron 窗口背景色与页面的 `theme-color` 曾经是同一个退役颜色的两处手工拷贝；它们现在跟随 `--c-chrome`，一旦漂移即由测试拦下。
- 重建应用外壳：导航侧栏变成 52px 图标轨道，顶栏从 76px 降到 40px 并承载品牌标记、会话标签与两个新开关，24px 状态栏报告连接状态、端点、终端尺寸与版本。
- 新增主题开关与行高密度开关。运行中的应用里浅色主题已经可达，两个选择都会被记住。因为页面的 Content-Security-Policy 禁止内联预绘制脚本，已存的浅色主题会在启动后一帧才生效。
- Hosts 与 Keychain 现在是带列标题行的表格：主机为名称、地址、用户、认证、最后保存日期；密钥为名称、类型、指纹、关联主机数、创建日期。卡片视图一键可达，且不再是默认视图。认证列写明密钥来自密钥库还是本机文件 —— 那正是决定一台主机能不能用的事实。
- 连接编辑器嵌入窗口而不是盖住主机清单，宽度与密钥编辑器同为 322px，两者现在共用同一个宽度 token。
- 各屏断点从六个收敛为三个（1100 / 820 / 620）。820 以下主机表格丢掉用户与认证两列，而不是把它们挤窄。
- 会话屏有了自己的工位：终端与远端文件表是两栏，中间是一条可拖的握把，而它是真正的分隔条（可聚焦、方向键可调、有夹持范围所以拖不出「一栏消失」），每个会话保留自己的比例。820px 以下文件表回到终端下方，而不是把窗口撑破。
- 远端目录改为四列表格 —— 名称、大小、模式、修改时间 —— 列标题与行共用同一份模板。模式取自栈一直返回、屏幕从没画过的权限位；对端没给属性时画成长横线而不是 `000`。表格上方是一串可点的面包屑，而路径输入框保留：去 `/var/log` 不该靠一层层点进去。
- 连接失败会说清停在哪一步：四个节点，死掉的那个标红，它之前的标为已通过，连接线在断点处断开。认不出的失败把整条路线收起，而不是画一排什么都没说明的点；阶段同时用文字复述一遍，因为那张图对辅助技术是隐藏的。
- 已完成的事件（保存、删除、后台会话连上了）改为右下角通知，点击即关、最多三条、live region 角色分得对；常驻说明与字段校验提示刻意留在原处。
- 主机列表有了与自己行等高的骨架，有了与「这台机器的能力关着」分得清的「这次没读出来」，以及重试入口。
- 让整个工作台对齐设计原型的控件尺度与排布。38px 下限在按钮、输入框、下拉框以及它们所在的 40px 工具栏上统一变为 28px；圆角阶梯收敛到控件用 `--r-1`、行用 `--r-2`、对话框用 `--r-3`，12px 的 `--r-4` 再无消费者。主机页与密钥库页的头部各自合成单独一条 40px 行，连接编辑器重切了它的分区、开关与底栏，会话工具栏承载失败结论，于是失败页不再重复它，顶栏品牌读作一行 —— `PureTerm / Vault`，分区名随当前所在的地方变 —— 而不再是上下堆叠的标题，空状态从文件表的行内占位中独立出来，快捷键对话框则添了标题栏与底栏。
- 顶栏按原型收尾。库标签从标题栏里退场：品牌里的 `/ Vault` 已经说明你在哪一屏，切库页面的是轨道上的 Hosts 那一格，所以那个标签是把一件事说了两遍 —— 而且它还是顶栏里唯一一个活动标签，其 `--line` 描边被一条同权重、位置更靠后的规则抵消掉，于是原型 `.tab.on` 的那道描边在它身上是缺的，它一走顶栏里就再没有缺描边的活动标签。品牌分区名改用原型的 `--tx-4` 档（在顶栏底面上暗色 2.79:1、浅色 2.28:1），由 `stylesheet-contract.test.mjs` 里一条点名且受审计的豁免放行 —— 装饰性地重复轨道与面板标题已经说过的事，是唯一扛得住这一档的文字位置。
- 顶栏的窗口按钮改为桌面端独有。最小化/最大化/关闭这一组从 `packages/ui/src/index.html` 与共享样式表里移出，改由第二份清单 —— `packages/ui/src/desktop.css`，作为独立的 esbuild 入口构建 —— 承载那四条规则；`services/chrome.ts` 链接这张表并在运行时建出这三个按钮，且只在桌面桥提供 `windowControls` 时这么做。独立 Web 入口于是既不带那份标记、也不带那些规则，它的构建产物 `app.css` 正是如此：其中没有任何规则提到 `.window-control`。macOS 什么都不画，保留自己的红绿灯 —— 桌面端平台计划里的 `drawsOwnWindowControls` 是回答「哪些平台自绘」的唯一一处，因此 Windows 与 Linux 保留原有的那一组，而 macOS 在标题栏覆盖层被移除、已没有为它预留的原生标题按钮之后，也不会在红绿灯旁边再多出一套。
- 让 SSH exec 有界且可取消，并加入监控协议。`exec` 接受中止信号与包含 channel 获取时间的截止时间，保留的 stdout 加 stderr 上限为 65,536 字节，超限是拒绝而不是返回部分结果；`ExecResult.truncated` 标记已移除。协议新增 `monitor:start`、`monitor:stop`、`monitor:update` 一对请求加事件流，以及没有请求侧的 `session:facts` 事件和配套校验规则。既有终端、SFTP 与主机操作不变。
- 在 `0.1.0-alpha.1` 之后合并的工作中持续更新此区段。

### Fixed

- 不再让会话把外壳拆开。以前连上主机会挂一个 `session-mode` 类，`chrome.css` 对它回应的是隐藏导航轨道并把主体收成单独一列，于是连上之后的画面读起来像主框架丢了，而不是变窄了。轨道现在在整个会话期间保持自己的 52px 轨宽，也照旧切换库页面 —— 它本来就一直能切，因为 Hosts 那一格和品牌区一直是回去的路 —— 而这个类与它的两处切换跟着规则一起删掉了，因为没有别的东西读它。原型画的每一屏外壳都是「轨道 + 主区（+ 检查器）」。

- 失败日志里的错误行从未被着色：那条规则涂的是一个日志从不生成的 `<i>` 元素。日志现在真的带一个标记，涂色规则也就第一次生效。

- 状态栏只显示后端真正报告的字段。协商加密算法与服务端主机密钥算法现在由 SSH 握手填入，不再属于空缺的那几格。会话自身的在线时长与传输速率仍然空缺而非近似填充：快照里的是**主机**的运行时间，是另一个数字，不能当成会话时长印出来；传输速率则根本没有通道。
- 删掉点名了不存在之物外壳样式：导航折叠开关及其折叠态、没有对应 markup 的窗口按钮规则、更新药丸，以及一个从未被任何代码添加的外壳状态类。
- `hosts.css` 里两个 `@media (max-width: 620px)` 块曾互相悄悄覆盖彼此的滚动模型；现在只剩一个。
- 两个库屏的视图开关过去描述的是你正要离开的视图，而不是你会得到的那个；而密钥库的策略句 —— 那句声明私钥材料从不以明文落盘的说明 —— 原本是标题下面的一行小字。现在它是一条横幅。

### Security

- 为 Desktop 窗口的准确 WebSocket 请求使用由主进程持有的独立 bearer token；不把它写入页面 URL、DOM、浏览器 cookie 或存储。

## [0.1.0-alpha.1] - 2026-09-16

### Added

- 提供使用独立 Node Host 子进程处理 SSH、SFTP、存储和 Cordis 服务的 Electron Desktop。
- Desktop IPC 与本机 Web 入口共用 Cordis Client 生命周期，包括作用域清理和浏览器缓存恢复。
- 提供 Windows NSIS、macOS DMG/ZIP 和 Linux AppImage 的 CI 构建产物。
- 通过 GitHub Releases 检查更新，并由用户确认重启安装。
- 提供仅限本机访问的 Web，浏览器凭据只在当前会话使用，不提供用户或租户隔离。

### Security

- Desktop 凭据继续由操作系统凭据存储保护，Host 进程通过私有 IPC 能力访问它们。
- 发布构建排除可选的原生 SSH 加速模块，不包含 GitHub 凭据。

[Unreleased]: https://github.com/puremixai/pureterm/compare/v0.1.0-alpha.1...HEAD
[0.1.0-alpha.1]: https://github.com/puremixai/pureterm/releases/tag/v0.1.0-alpha.1
